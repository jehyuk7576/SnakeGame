<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a35study">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.35study">
        <activity android:name=".SnakeElement"></activity>
        <activity android:name=".RoomWall" />
        <activity android:name=".GameEngine" />
        <activity android:name=".BoardComponent" />
        <activity android:name=".Board" />
        <activity android:name=".AppleElement" />
        <activity android:name=".GameStartActivity" />
        <activity android:name=".GameActivity" />
        <activity android:name=".SnakeGameView" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>